# 智慧矿山指挥调度平台 — 接口评审记录 v1.0

> 版本：v1.0 | 日期：2026-03-29
> 状态：待三方签字

---

## 1. 内部接口

### 1.1 事件总线接口

```go
// EventBus 事件总线（internal/eventbus/bus.go）
type EventBus interface {
    Subscribe(topic string, ch chan Event)
    Unsubscribe(topic string, ch chan Event)
    Publish(e Event)
    Close()
}

// 事件类型
const (
    EventTypeAlert    = "alert"     // 报警事件
    EventTypeDispatch = "dispatch"  // 调度事件
    EventTypeCommand  = "command"   // 指令事件
    EventTypePlan     = "plan"      // 预案事件
)
```

### 1.2 调度内核接口

```go
// Dispatcher 调度器（internal/dispatch/dispatcher.go）
type Dispatcher interface {
    // HandleAlert 处理报警
    HandleAlert(ctx context.Context, alert Alert) error
    // SendCommand 下达指令
    SendCommand(ctx context.Context, cmd Command) error
    // ExecutePlan 执行预案
    ExecutePlan(ctx context.Context, planID string, event Event) ([]Command, error)
}
```

### 1.3 AI服务接口

```go
// AIService AI分析（internal/ai/service.go）
type AIService interface {
    // AnalyzeAlert 分析报警风险
    AnalyzeAlert(ctx context.Context, alert Alert) (*AIRecommend, error)
    // RecommendPlan 推荐处置方案
    RecommendPlan(ctx context.Context, alert Alert) ([]PlanRecommend, error)
    // SearchKnowledge 检索知识库
    SearchKnowledge(ctx context.Context, query string) ([]KnowledgeItem, error)
}
```

### 1.4 存储层接口

```go
// Store 存储层（internal/store/store.go）
type Store interface {
    // Alert
    SaveAlert(ctx context.Context, alert *Alert) error
    GetAlert(ctx context.Context, id string) (*Alert, error)
    ListAlerts(ctx context.Context, query AlertQuery) ([]Alert, error)
    UpdateAlertStatus(ctx context.Context, id, status string) error

    // Plan
    SavePlan(ctx context.Context, plan *Plan) error
    GetPlan(ctx context.Context, id string) (*Plan, error)
    ListPlans(ctx context.Context) ([]Plan, error)

    // Command
    SaveCommand(ctx context.Context, cmd *Command) error
    UpdateCommandStatus(ctx context.Context, id, status string) error

    // Knowledge
    SaveKnowledge(ctx context.Context, item *KnowledgeItem) error
    SearchKnowledge(ctx context.Context, query string) ([]KnowledgeItem, error)
}
```

---

## 2. 外部接口

### 2.1 WebSocket 实时推送

```
GET /ws?token=xxx

// 服务器推送消息格式
{
    "type": "alert|command|status",
    "data": {...},
    "traceId": "xxx",
    "timestamp": "2026-03-29T12:00:00Z"
}
```

### 2.2 kj90x 协议接入

```
TCP Server :54321

// 接收格式（蛟洋kj90x协议）
$KJ90,ALM,设备地址,气体类型,浓度,时间,状态*XX

// 转换为内部Alert事件
Alert{
    Source: "kj90x",
    Level: computeLevel(gasType, value),
    Location: deviceAddress,
    GasType: gasType,
    Value: value,
    Time: parseTime(time),
}
```

### 2.3 REST API（HTTP）

见技术方案第5节。

---

## 3. 签字确认

| 角色 | 确认内容 | 签字 |
|------|---------|------|
| 前端RD | 3D一张图接口 + WebSocket推送协议 | 待签 |
| 后端RD | EventBus + 调度内核 + AI服务接口 | 待签 |
| PM | 整体功能范围 + 优先级 | 待签 |

---

## 4. 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| kj90x协议解析 | 依赖蛟洋协议文档 | 先仿真测试，接口抽象 |
| 3D性能 | 大规模矿山模型渲染 | 分片加载，LOD优化 |
| AI准确性 | LLM幻觉导致误判 | 知识库约束，置信度阈值 |
| 实时性 | 高并发报警风暴 | 消息队列缓冲，降级策略 |
